export class Paths{
    static LOGIN = 'login';
    static AUTH_CHECK = 'authCheck';
}

export const config = {
    timeout: 5000,
    headers: {
        'Content-Type': 'application/json',
        'Accept': 'application/json'
    }
};